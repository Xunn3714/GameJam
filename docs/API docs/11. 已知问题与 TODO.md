# 已知问题与 TODO

> **Status: Draft**
> **Owner: TBD**
> **Last updated: 2026-09-04**

## 已确定

- [x] MVP采用2D俯视角。
- [x] 首轮测试平台为Windows。
- [x] 玩家使用WASD移动，Space拉屎。
- [x] MVP关卡使用 `Level_01`，目标为招募另外5只羊。
- [x] `ProjectSettings/` 继续纳入版本控制，但羊群 MVP 不修改它。

## 羊群 MVP 待办

- [ ] 在 `InputSystem_Actions.inputactions` 增加 `Player/Poop`，并在玩法稳定后删除无关模板 Action。
- [ ] 创建个人 `SheepMvp_Dev` Scene，不直接在共享 `Level_01` 开发。
- [ ] 实现玩家移动、随机名称、招募、跟随、任务 HUD、拉屎和结算统计。
- [ ] 为名称生成、任务进度和本局统计编写 EditMode 测试。
- [ ] 由 `Level_01` owner 完成 Prefab 和 HUD 集成。
- [ ] 在 Unity 中完成编译、归档测试场景和 MainMenu → AlphaFlockExpansion 流程验证。

## 当前仓库问题

- [ ] `Assets/_Game/Content/Perfabs` 拼写与规范中的 `Prefabs` 不一致；由 owner 在 Unity 中统一处理。
- [ ] `PixelArtImporter` 只匹配 `Art/Sprites`，与当前 `Art/Characters`、`Art/Environment` 目录不一致。
- [ ] 归档的 `Old/Tests/SampleScene` 引用仓库中不存在的 `GameJam.Game.UI` 脚本；确认资产来源或移除废弃引用。
- [ ] 归档的 `Old/Legacy/Ending` 引用了仓库中不存在的 `CameraShake2D`。
- [ ] 确认不需要 Unity Version Control、Visual Scripting、Multiplayer Center 后再精简 Packages。

## 暂缓

- asmdef：现有 `SceneLoader`、`AudioManager` 和 `PauseManager` 位于 `Assembly-CSharp`；迁移现有代码前，不为羊群模块单独增加 asmdef。
- 正式分辨率、发布平台和构建方式：待 MVP 试玩通过后确定。
