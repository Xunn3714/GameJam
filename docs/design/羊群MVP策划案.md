# 《找到另一只羊》MVP 策划案

- **状态**：Draft
- **Owner**：TBD（策划）
- **最后更新**：2026-09-04
- **关联技术方案**：[羊群 MVP 技术方案](<../tech docs/羊群MVP技术方案.md>)
- **Agent 入口**：[`AGENTS.md`](../../AGENTS.md)

## 1. 一句话概念

玩家控制一个最初只有 1 名成员的羊群，通过成员接触其他羊让对方加入，最终形成一个拥有名字和共同旅程记录的小族群。游戏中没有固定头羊。

## 2. MVP 要验证什么

第一版只验证三个问题：

1. 从一只羊逐渐变成一群羊，过程是否有满足感。
2. 新成员加入时，名字、队伍变化和 UI 反馈是否足够明确。
3. “拉屎”能否作为简单、有辨识度的初始主动技能。

如果玩家愿意主动寻找最后一只羊，并且看到队伍扩大、结算成员名单时感到有趣，核心方向即成立。

## 3. 玩家流程

```text
进入 Level_01
→ 建立一个包含 1 只随机命名成员羊的族群
→ 看到任务“找到另外 5 只羊”
→ 使用 WASD 探索草地
→ 接触一只羊，对方加入族群
→ 显示该羊名称和任务进度
→ 使用 Space 拉屎
→ 找齐 5 只羊
→ 查看本局统计和成员名单
→ 重新开始或返回主菜单
```

预计单局时长为 3～5 分钟。

## 4. 确定的游戏规则

### 4.1 移动

- 游戏采用 2D 俯视角。
- 玩家使用 WASD 控制羊群中心移动，而不是控制某一只羊。
- 第一版不需要冲刺、攻击或交互键；人数障碍由接触时自动判断，不增加主动冲撞按键。
- 所有成员羊地位相同，围绕族群中心自动移动，不存在固定头羊。
- 成员持续通过避让、聚拢、速度趋同和低频游走形成动态羊群，不锁定固定相对位置。
- 停止输入后关闭随机游走，成员只完成必要的避让和归群，然后稳定静止。

### 4.2 招募

- 玩家开局控制一个拥有 1 名成员的族群。
- 场景中放置 5 只待招募羊。
- 任意族群成员接触待招募羊后，对方立即加入族群，不需要按键。
- 每只羊只能被招募一次。
- 任务进度统计新找到的羊，范围为 `0/5` 至 `5/5`。
- 族群数量包含初始成员，范围为 1 至 6。
- 第一版没有失败条件，也不会主动触发成员丢失；数据模型允许后续移除成员，而不依赖某个特定成员存活。

### 4.3 随机名称

- 初始成员羊和 5 只待招募羊在每局开始时获得名称。
- 单局内名称不重复。
- 名称只在加入提示和最终成员名单中显示，不常驻羊的头顶。
- 重新开始后重新随机。
- 名称不跨局保存。

初始名称池建议：

```text
棉花糖、云朵、小卷、豆豆、奶盖、白团
咩咩、毛球、盐巴、小雪、软糖、月亮
```

名称池不足时使用 `小羊 01`、`小羊 02` 等后备名称。

### 4.4 人数障碍

- 场景放置一个简单长方形障碍。
- 族群数量不超过 5 时，羊群中心无法穿过，碰撞时障碍短暂变红。
- 族群数量大于 5 时，任意成员或羊群中心撞到障碍都会将其破坏。
- 障碍只验证“壮大族群后解锁道路”的反馈，不加入伤害、耐久或复杂破坏动画。

### 4.5 拉屎技能

- 按键：Space。
- 开局立即可用。
- 冷却时间：2 秒。
- 使用成功后在羊身后生成一坨粪便。
- 粪便存在10秒，场上最多同时存在10个。
- 冷却中、暂停时或游戏完成后不能使用。
- 第一版粪便没有伤害、减速或施肥效果。

该功能目前用于测试主动技能的基本体验：按键、生成、反馈、冷却和统计。生态效果留给后续版本。

## 5. UI 与文案

### 5.1 常驻 HUD

```text
找到羊：0/5
族群：1
Space：拉屎
```

冷却时将技能提示改为：

```text
拉屎：1.4秒
```

### 5.2 加入反馈

招募成功后显示约1.5秒：

```text
“棉花糖”加入了族群！
```

同时应有至少一种非文字反馈：简单音效、缩放、高亮或粒子。

### 5.3 完成与结算

找齐第5只羊后显示：

```text
你找到了所有羊！

族群规模：6
拉屎次数：12
游戏用时：01:43
成员：云朵、豆豆、奶盖、白团、毛球、小雪

[重新开始]  [返回主菜单]
```

第一版统计字段固定为：

- 族群规模。
- 成员名单。
- 成功拉屎次数。
- 游戏用时。

暂停时间不计入游戏用时，冷却期间的无效按键不计入拉屎次数。

## 6. 场景设计

第一版使用 `Level_01`，玩法区域约为20×14个世界单位。

建议位置：

| 对象 | 参考坐标 | 作用 |
|---|---|---|
| 羊群中心 / 初始成员 | `(0, 0)` | 开局位置 |
| 羊1 | `(-4, 2)` | 最容易发现，教学招募 |
| 羊2 | `(4, 3)` | 引导向右上探索 |
| 羊3 | `(-6, -3)` | 引导向左下探索 |
| 羊4 | `(6, -2)` | 较远目标 |
| 羊5 | `(1, 6)` | 最后探索区域 |

场景使用树、石头、草丛或围栏引导路线，但不设置会卡住跟随羊的狭窄通道。最后一只羊不能隐藏得过深。

## 7. 体验节奏

| 阶段 | 玩家感受 | 主要反馈 |
|---|---|---|
| 开局 | 孤单、目标明确 | 只有一只羊，提示寻找同伴 |
| 第一次招募 | 立即理解规则 | 名称提示、数量变化、开始跟随 |
| 中段 | 族群逐渐壮大 | 队伍视觉变大，可随时拉屎 |
| 最后一只羊 | 有寻找目标 | 任务进度接近完成 |
| 结算 | 形成一次小旅程 | 成员名单和本局统计 |

## 8. MVP 验收标准

- 新玩家不看外部说明也能理解 WASD 移动和寻找羊的目标。
- 第一次接触羊时能明确感知“它加入了我”。
- 玩家能分辨任务进度和族群总数的区别。
- 羊群扩大后具有明显的视觉满足感。
- 玩家知道 Space 可以拉屎，并能理解冷却状态。
- 找齐5只羊后能看到正确的成员名单与统计。
- 一局能够在5分钟左右完成，不因找不到最后一只羊而长时间停滞。

## 9. 第一版不做

- 主动冲撞、战斗及其他群体技能。
- 牛、猴子或其他动物。
- 生老病死、繁殖、疾病、饥饿。
- 随机事件和复杂生态系统。
- 战斗、敌人和失败条件。
- 多关卡、人类相遇和分支结局。
- 名称自定义、本地化和跨局保存。
- 排行榜、成就和长期统计。

## 10. 后续评估方向

完成 MVP 试玩后再决定：

- 羊越多时解锁的第一个群体能力是什么。
- 拉屎是否发展为施肥、减速或路线标记。
- 是否加入食物、疾病和死亡压力。
- 第二个物种应选择牛还是猴子。
- 完成 Level_01 后是否进入 Ending。

这些内容不应提前进入第一版实现范围。
